<!-- Describe Auth usage -->

## API Consumption Auth Flow
On Login, api returns an access and refresh token. Access token is used to access the api. Refresh token is used to get a new access token when the access token expires.

